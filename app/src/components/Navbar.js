import styled from "styled-components";

const Container = styled.div`
    display: flex;
    color
    height: 11
`;
const Navbar = () => {
    return(
        <Container>

        </Container>
    )
}
